# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# Virtual environments / uv
.venv/
venv/

# uv: keep uv.lock committed for reproducible deploys; ignore the cache only
.uv-cache/

# Dataset (large / regenerable from Kaggle + Roboflow). See docs/labeling-guide.md.
data/

# Ultralytics training artifacts and auto-downloaded pretrained base weights.
runs/
runs_*.log
/yolo*.pt
*.onnx
*.cache

# Roboflow / training exports, and the data.yaml generated from a local export
# (it stores an absolute path into the git-ignored datasets/ dir).
datasets/
/data.yaml

# NOTE: the final deployed weights under models/ (e.g. models/best.pt) ARE tracked
# on purpose so Streamlit Community Cloud can load them. Keep them small (YOLO11s/n).

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Streamlit secrets
.streamlit/secrets.toml

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Environment
.env
