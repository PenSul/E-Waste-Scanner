"""Streamlit UI components: detection overlay and dashboard rendering.

These keep ``app/streamlit_app.py`` a thin entrypoint by holding the drawing
and layout logic. They depend only on domain/service objects, never on adapters.
"""
